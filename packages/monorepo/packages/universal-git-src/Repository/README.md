# class Repostory 
Combines gitBackend with worktreeBackend and executes 

## on gitBackend

.discoverCapabilitys() => {
    config.objectFormat,
    ....,
    namespaces,
    worktrees,
    submodules,
}