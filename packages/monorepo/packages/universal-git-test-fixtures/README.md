# Test Fixtures Package

This package contains shared test fixtures used by all universal-git test packages.

## Usage

Import fixtures using the package:

```typescript
import fixtures from '@awesome-os/universal-git-test-fixtures'
```

Fixtures are located in the `fixtures/` directory and can be accessed via the `makeFixture` helper from `@awesome-os/universal-git-test-helpers`.

