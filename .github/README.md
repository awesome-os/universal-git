## awesome-os/universal-git#ismorphic-git-main-patch
This branch mainly tracks all upstream fixes that we apply on continouse base to reuse the old source till we got replacement code.

- Corrects Typos:
  - FSClient should be FsClient
  - src/index.js should not have a default or namespace export.
  - package.json needs patching.
